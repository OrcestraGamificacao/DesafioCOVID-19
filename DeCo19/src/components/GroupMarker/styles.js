import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    smallMarker: {
        width: 16,
        height: 16,
        borderRadius: 8,
        borderWidth: 2,
        borderColor: 'white'
    }
});

export default styles;