import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    
   virusTitle: {
      fontSize: 16,
      paddingTop: '10%',
   },

   infoInfected: {
      fontSize: 16,
      fontStyle: 'italic',
      textAlign: 'right',
      paddingRight: '2%',
   },

   image: {
      alignSelf: 'center',
   }
});

export default styles;